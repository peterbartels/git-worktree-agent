/target
/dist

# Per-user worktree manager config (different per developer)
.gwm-config.json

# Logs
*.log
gwm-debug.log

# Node
node_modules/
package-lock.json

# Compiled binaries (downloaded by npm install)
# Note: bin/gwm (the JS wrapper) is tracked in git
bin/gwm-binary
bin/gwm-binary.exe

.env
