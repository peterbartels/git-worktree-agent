/target
/dist

# Per-user worktree agent config (different per developer)
.gwa-config.json

# Logs
*.log
gwa-debug.log

# Node
node_modules/
package-lock.json

# Compiled binaries (downloaded by npm install)
# Note: bin/gwa (the JS wrapper) is tracked in git
bin/gwa-binary
bin/gwa-binary.exe

.env
